language: cpp

before_install:
  - sudo apt install nasm -y
  - sudo apt install g++ -y
  - sudo apt install make -y
  - sudo apt install gcc -y
  - sudo apt install xorriso -y
  - sudo apt install grub2 -y
  - sudo apt install grub-legacy-ec2 -y

script:
  - make clear
  - make kernel
  - make loader
  - make link
  - make iso
